#scan the network an dif possible find out hostname of the ip address
#first thep define the ip adress range 
    #needs to have 10.x.x.x - needs user input
    #get the x on for loop 
#secend ping them 
#third if pingable get hostname and save it to csv

$get_ip = Get-IpamConfiguration